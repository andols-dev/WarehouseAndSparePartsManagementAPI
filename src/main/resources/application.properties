spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=WarehouseDb;encrypt=false
# SQL Server credentials - demo/development only
# Do not use these credentials in production
spring.datasource.username=sa
spring.datasource.password=MyStrongPass123!

spring.jpa.hibernate.ddl-auto=validate

spring.flyway.enabled=true

# JWT secret key - demo/development only
# Do not use this secret in production
jwt.secret=MyDemoJwtSecretKeyForDevelopmentOnly123456789

